/**
 * Calculation function is produces x + y
 * @param {number} x
 * @param {number} y
 * @returns {number}
 */

const calculation = (x: number, y: number): number => {
  return x + y;
};

export default calculation;
